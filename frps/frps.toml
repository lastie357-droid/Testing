bindAddr = "0.0.0.0"
bindPort = 7000

# Must match frpc — disable TCP multiplexing for lower latency
transport.tcpMux = false

webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "admin"
transport.heartbeatTimeout = 200